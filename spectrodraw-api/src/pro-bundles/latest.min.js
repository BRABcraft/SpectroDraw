export default `

export async function initSpectroDrawPro() {
  const h1 = document.createElement('h1');
  h1.textContent = 'pro loaded';
  h1.style.position = 'fixed';
  h1.style.bottom = '1rem';
  h1.style.right = '1rem';
  h1.style.fontFamily = 'sans-serif';
  h1.style.fontSize = '1.5rem';
  h1.style.color = '#0f0';
  h1.style.background = '#000';
  h1.style.padding = '0.5rem 1rem';
  h1.style.borderRadius = '0.5rem';
  h1.style.zIndex = '9999';
  document.body.appendChild(h1);
  console.log('SpectroDraw Pro bundle initialized.');
}

// Automatically call the init function when imported
initSpectroDrawPro();

`;